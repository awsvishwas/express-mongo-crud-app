<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Players</title>
</head>
<body>
    <h1>List of <%= category %> Players</h1>
    <ul>
        <% for(player of players) { %>
        <li><a href="/players/<%= player._id %>"><%= player.name %></a></li>
        <% } %>
    </ul>
    <a href="/players/new">Add Player</a>
    <% if (category!=='All') { %> <a href="/players">All Players</a><% } %>
</body>
</html>