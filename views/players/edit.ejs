<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Player Information</title>
</head>
<body>
    <h1>Edit Player Information</h1>
    <form action="/players/<%= player._id %>?_method=PUT" method="post">
        <label for="name">Name: </label>
        <input type="text" id="name" name="name" value="<%= player.name %>" ><br><br>

        <label for="age">Age: </label>
        <input type="number" id="age" name="age" value="<%= player.age %>" required><br><br>

        <label for="role">Role: </label>
        <select id="role" name="category"  required>
            <% for(let category of categories) {%>
            <option value="<%= category %>" <%= player.category === category? 'selected': '' %> > <%= category %> </option>
            <% } %>
        </select><br><br>

        <input type="submit" value="Submit">
        <a href="/players/<%= player._id %>">Go Back</a>
    </form>
</body>
</html>
